name: Deploy n8n to Free Hosting

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  # این workflow فقط برای تست است
  # برای deploy واقعی از Railway/Render/Fly.io استفاده کنید
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Test Docker Build
        run: |
          docker build -t n8n-test .
          echo "✅ Docker image built successfully"
      
      - name: Info
        run: |
          echo "📝 برای deploy واقعی، از Railway.app یا Render.com استفاده کنید"
          echo "🔗 راهنمای کامل در فایل DEPLOY-FREE.md موجود است"

